// Loosely-typed Supabase client for use until auto-generated types catch up.
// Import this instead of `client` when the generated types are empty.
import { supabase } from './client';

export const db = supabase as any;
